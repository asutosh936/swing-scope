# Tests run against an in-memory database so they never touch the real ./data file.
spring:
  datasource:
    url: jdbc:h2:mem:swing-scope-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
